<snippet>
	<content><![CDATA[
qx.Class.define("${1:namespace.Class}", {
  
  extend : qx.ui.mobile.page.NavigationPage,

  construct : function() {
    this.base(arguments);
  },

  events : {
  },

  properties : {
  },

  members : {
  	_initialize : function() {
  		this.base(arguments);
  	},

  	_start : function() {
  		this.base(arguments);
  	},

  	_stop : function() {
  		this.base(arguments);
  	},

  	_pause : function() {
  		this.base(arguments);
  	},

  	_resume : function() {
  		this.base(arguments);
  	},

  	_wait : function() {
  		this.base(arguments);
  	},

  	_onBackButtonTap : function() {
  	}
  }
});
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>qx.Class.define.NavigationPage</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.js</scope>
</snippet>
